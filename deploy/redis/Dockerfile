# this image contains server and its configuration only
# server from this image will be run from docker-compose.yaml
FROM europe-central2-docker.pkg.dev/aluminium-prediction/aluminium-prediction/redis-base

# copy configuration
COPY configs/redis/redis.conf /usr/local/etc/redis/redis.conf

